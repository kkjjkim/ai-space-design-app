# PROGRESS

## 끝낸 것
- [x] 0단계 — 셋업 전환: pnpm 전환, Prisma/관리자 제거, 루트 `app/components/lib` 구조,
      디자인 토큰(AvroKO: 아이보리/차콜/골드 + Noto Serif KR/Pretendard), shadcn/ui 기본 컴포넌트,
      Supabase 클라이언트, zod 스키마, `.env.example`, ESLint
- [x] 1단계 — 공통 레이아웃: 상단 내비(`site-header`) + 스크롤 고정 CTA(`floating-cta`) + 푸터
- [x] 2단계 — 메인 원페이지(섹션 1~8): 히어로/공감/진짜문제/비교표/우리방식/증거+포트폴리오/가격뒤집기/신청폼
      (카피는 PRD 그대로)
- [x] 3단계 — 서브 페이지: 포트폴리오 / 회사·신뢰 / 서비스 / 신청 완료
- [x] 4단계 — 폼 + Supabase 저장 + Resend 알림(`/api/leads`). 키 없으면 데모 모드로 흐름만 동작.
- [x] 검증 루프: typecheck / lint / build 통과
- [x] 디자인 다듬기 — AvroKO 벤치마크 반영: 히어로 에디토리얼 레이아웃
      (좌 대형 명조 헤드라인 + 우 골드 이탤릭 인용구), 풀블리드 슬라이더 캡션 바
      (둥근 화살표 · 프로젝트명 · "01 OF 08"), 레터스페이싱 로고
- [x] 비주얼 중심 재구성 (ZORGE 시네마틱 벤치마크) — 텍스트 과다 해소
      · 시네마틱 풀블리드 영상 히어로(SiteHero) + 어두운 히어로 위 밝은 헤더
      · 풀블리드 시네마틱 밴드(CinematicBand): 진짜문제 / 가격 섹션
      · 우리 방식 = 이미지 타일 4단계, 공감 = 이미지+카피 분할
      · MediaFrame 공용 슬롯, 포트폴리오 데이터화(lib/portfolio.ts)
- [x] 컨셉 제안 5종(SORA/MELLOW/EMBER/NEST/PLATE) — lib/concepts.ts,
      홈 갤러리 + /concepts 상세 페이지. "실제 시공 사례 아님(컨셉 예시)" 명시 (AGENTS.md 7번 준수)
- [x] public/ASSETS.md — 영상/컨셉이미지/실사진 드롭인 가이드

## 검색 노출 작업 (2026-09-18)
- [x] 1단계 — 한글 브랜드명 '브랜드각' 병기: 전 페이지 타이틀·OG·구조화 데이터.
      화면 로고(PAUL & MARIE)는 그대로. 이전엔 사이트에 '브랜드각'이 0회라 브랜드 검색 유입이 0이었다.
- [x] 2단계 — 자격 신호: 경영지도사·실내건축공사업 면허·R&D 전담부서를 hasCredential 로.
      글 감수자는 실명 없이 직함만(대표님 결정).
- [x] 3단계 — BreadcrumbList(전 페이지), 인사이트 Blog 묶음 스키마, RSS 피드 `/feed.xml`(21편).
- [ ] 4단계 — 광고 추적 배관: 구글 애즈 전환 ID·라벨, 메타 픽셀. **ID 발급 필요(대표님)**
- [ ] 5단계 — 네이버 계정 작업(코드 아님): 플레이스 업체 등록, 블로그 개설,
      서치어드바이저에 사이트맵·RSS 수집 요청. 네이버 일반 키워드 노출의 대부분이 여기서 나온다.
- [x] 인사이트 6편 추가 (2026-09-18, 총 27편) — 상권분석 / 인테리어 계약서 / 권리금 /
      미용실·뷰티샵 / 음식점 인허가 / 매장 리뉴얼. 커버는 기존 컨셉 이미지 재사용(전용 커버 미제작).
- [x] 배포 완료 (2026-09-18) — 브랜드명·구조화 데이터·RSS·새 글 6편 프로덕션 반영 확인
- [x] 컨셉 진단 도구 `/diagnosis` — 5문항 → 단계별 처방 + 상담 폼 자동 채움.
      **왜 만들었나:** 가치잇다가 네이버에 잘 뜨는 건 SEO 실력이 아니라 "점포매매"라는
      빈 카테고리의 주인이라서다(타이틀의 정확한 문구에서만 노출, 동의어 "가게 매매"는 0건).
      브랜드각이 노리는 "창업 컨설팅"·"매장 인테리어"는 포화라 같은 수법이 안 통한다.
      그래서 경쟁 키워드 대신 도구로 유입·전환을 만드는 쪽으로 전환.
- [x] 업종별 랜딩 5종 `/interior/*` (미용실·베이커리·대형카페·레스토랑·리테일).
      검색어를 타이틀 맨 앞에, 업종이 미리 채워진 상담 폼으로 끝남. /service 에서 링크.
- [x] 네이버 서치어드바이저 등록 완료 (대표님, 9/18) — 웹페이지 수집 6건, RSS 제출,
      사이트맵은 7/6부터 이미 등록돼 있었음.
- [x] 전용 이미지 7장 생성·배포 (GPT Image 2, Higgsfield MCP).
      미용실 랜딩 히어로 `/interior/beauty-salon.jpg` + 새 글 6편 커버.
      JPEG 리사이즈로 7장 합계 1MB. 인사이트 27편 커버가 전부 고유해짐.
- [x] 최근신청 토스트 수정 — 경과일 표기 제거(대표님 선택), 업종에 한글이 없는 값은
      라벨을 비움("Technology" 등 테스트·스팸). 신청 데이터는 그대로 두고 표시만 손봤다.

## 답변 엔진 대응 (AEO, 2026-09-20)
- [x] 질문·답 허브 `/answers` + 질문별 페이지 13개.
      **왜:** 사람은 "권리금, 얼마가 적정한지 판단하는 기준"으로 검색하지만
      챗지피티에는 "권리금 계산 어떻게 해요?"라고 묻는다. 답변 엔진은
      (1) 질문과 제목이 같고 (2) 첫 문단이 곧 답이고 (3) QAPage 구조화 데이터가 붙은
      페이지를 인용한다. 기존 인사이트 27편은 이 형식이 아니라 인용되기 어려웠다.
      각 페이지: 짧은 답(인용되는 문단) → 표·번호 목록 → 브랜드각이 보는 방식 →
      관련 글·질문 → 질문이 미리 채워진 상담 폼.
      질문 13개: 권리금 계산/회수/무권리, 창업자금 배분, 카페 창업비용, 평당 단가,
      견적서 확인, 공사 기간, 음식점 인허가, 상가 계약, 상권분석, 프랜차이즈 비교, 정책자금.
- [x] `askedAs`(같은 질문의 다른 표현)를 구조화 데이터 alternateName + 키워드에 반영.
- [x] `robots.ts` — GPTBot·OAI-SearchBot·ClaudeBot·PerplexityBot·Google-Extended 등
      답변 엔진 크롤러를 이름으로 허용(의도를 남기기 위해). `/complete`는 계속 제외.
- [x] `/llms.txt` — 사이트가 무엇이고 어느 질문에 답하는지 한 장 요약.
      "금액을 단정하지 않는다"는 인용 시 주의사항도 함께 적었다.
- [x] 헤더 대비 버그 수정 — 어두운 히어로 판정이 `/`, `/service`, `/company`, `/concepts`만
      이어서 `/insights`·`/interior`·`/diagnosis`·`/answers`에서는 어두운 사진 위에
      짙은 글자가 얹혀 메뉴가 안 보였다. 접두사 판정으로 바꿨다.
- [x] 빙 색인 배관 (2026-09-20 추가) — 챗지피티 검색은 자체 색인(OAI-SearchBot)과
      **빙 계열 색인을 함께** 쓴다. 크롤러 허용만으로는 색인에 안 들어가서 이 둘을 넣었다.
      · `/indexnow-key.txt` + `pnpm indexnow` — 배포된 사이트맵을 빙·네이버·얀덱스에 즉시 통보.
        한 곳에 보내면 참여 검색엔진끼리 공유한다. 구글은 IndexNow 미참여(서치콘솔이 대신).
        `--dry-run` 으로 무엇이 나갈지 먼저 확인 가능. 키 파일이 안 열리면 전송하지 않는다.
      · `lib/site.ts` 에 빙 소유확인 코드 자리(`verification.bing`) — 값이 있을 때만 태그가 나간다.
- [x] `.github/workflows/indexnow.yml` — 버셀 프로덕션 배포가 성공하면 깃허브가
      자동으로 통보를 보낸다. 사람이 배포 후 명령어를 따로 치는 단계를 없앴다.
      Actions 탭에서 수동 실행(+dry-run)도 가능. 버셀 깃허브 연동이 꺼져 있으면
      신호가 안 와서 안 돈다 — 그때는 Actions 탭에서 손으로 돌리면 된다.
- [ ] **대표님 작업:** 빙 웹마스터 도구(bing.com/webmasters) 사이트 등록.
      구글 서치콘솔이 이미 인증돼 있으니 "구글 서치콘솔에서 가져오기"를 쓰면
      확인 코드 없이 끝난다. 이게 돼야 `pnpm indexnow` 통보가 색인으로 이어진다.
- [ ] 다음: 권리금·창업자금 계산 도구(입력값만 쓰고 숫자는 지어내지 않음)
- [ ] 다음: 기존 인사이트 27편 맨 위에 "짧은 답" 블록 얹기
- [ ] 다음: 외부 인용 늘리기(네이버 블로그·유튜브에서 이 페이지들을 인용) — 코드 아님

## 남은 것
- [ ] 콘텐츠 주기 유지: 7/6~9/18 사이 2개월 반 공백이 있었다. 주기가 끊기면 순위가 밀린다.

## 다음 할 것 (운영자/대표님)
- [ ] Supabase 프로젝트 생성 → `supabase/schema.sql` 실행 → `.env.local` 에 URL/anon 키 입력
- [ ] Resend 가입 → 도메인 인증 → `RESEND_API_KEY`, `LEAD_NOTIFY_TO`, `LEAD_NOTIFY_FROM` 입력
- [ ] `lib/site.ts` 의 `[브랜드명]`, 실적 숫자 `[ ]` 채우기 / 로고 교체
- [ ] 히어로 배경 이미지·영상, 포트폴리오 실사진(로고 없는 컷) 넣기
- [ ] OG 대표 이미지(`/og.jpg`)와 `metadataBase` 배포 도메인 교체
- [ ] Vercel 배포 + 환경변수 등록

## 지켜야 할 선 (AGENTS.md)
- "비교견적" 기능·문구 금지 / AI 가짜 인테리어를 실제 사례처럼 쓰지 않기
- 비밀키는 환경변수로만, service_role 키는 서버에서만
