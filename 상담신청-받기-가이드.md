# 상담 신청 받기 — 설정 가이드

> 지금은 **데모 모드**라 폼을 제출해도 아무데도 저장되지 않습니다.
> 아래 3단계를 하면 **① 신청이 오면 이메일로 즉시 알림 + ② Supabase에 목록으로 저장**됩니다.
> 코드는 손댈 필요 없고, 키 몇 개만 발급받아 Vercel에 넣으면 끝이에요. (약 15분)

---

## 1단계 · Supabase (신청 목록 저장)

1. https://supabase.com 접속 → **Start your project** → GitHub 계정으로 로그인
2. **New project** 클릭 → 이름 아무거나(예: `paulnmarie`), 비밀번호 아무거나 정하고 생성
   (지역은 **Northeast Asia (Seoul)** 추천, 생성에 1~2분 걸림)
3. 왼쪽 메뉴 **SQL Editor** → **New query** → 레포의 `supabase/schema.sql` 내용을
   **통째로 복사해 붙여넣고 → RUN**
   → `leads` 테이블이 만들어집니다. (신청 저장용, 외부에서 몰래 읽지 못하게 보안 설정 포함)
4. 왼쪽 메뉴 **Settings(톱니) → API** 로 가서 아래 2개를 복사해 둡니다:
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** 키 → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
     ⚠️ **service_role** 키는 절대 쓰지 마세요. (anon 키만 필요)

> 📋 **신청 목록 보는 곳**: Supabase → **Table Editor → leads** 테이블.
> 신청이 들어오면 여기에 한 줄씩 쌓입니다. (최신순)

---

## 2단계 · Resend (이메일 알림)

1. https://resend.com 접속 → **가입** (신청 알림을 받을 메일인 **kkjjkim@gmail.com** 으로 가입하세요)
2. 로그인 후 왼쪽 **API Keys** → **Create API Key** → 이름 아무거나 → 생성
   → `re_` 로 시작하는 키를 복사 → `RESEND_API_KEY`
3. 받는 사람 = 가입한 메일 그대로 `LEAD_NOTIFY_TO` = `kkjjkim@gmail.com`

> ⚠️ **무료 플랜 주의**: 도메인 인증 전에는 **가입한 본인 메일로만** 발송돼요.
> kkjjkim@gmail.com 으로 가입 → 같은 주소로 받으면 되니 지금은 문제 없습니다.
> 나중에 회사 도메인(@paulnmarie.com 등)에서 보내고 싶으면 Resend에서 도메인 인증 후
> `LEAD_NOTIFY_FROM` 을 그 주소로 바꾸면 됩니다.

---

## 3단계 · Vercel에 키 넣고 재배포

1. https://vercel.com → 이 프로젝트(`ai-space-design-app`) 클릭
2. **Settings → Environment Variables**
3. 아래 값들을 하나씩 추가 (Name / Value 입력, Environment는 **Production** 체크):

   | Name | Value |
   | --- | --- |
   | `NEXT_PUBLIC_SUPABASE_URL` | 1단계에서 복사한 Project URL |
   | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 1단계 anon public 키 |
   | `RESEND_API_KEY` | 2단계 `re_...` 키 |
   | `LEAD_NOTIFY_TO` | `kkjjkim@gmail.com` |

4. 저장 후 **Deployments** 탭 → 맨 위 배포의 **⋯ → Redeploy** (키를 반영하려면 재배포 필요)

---

## 마지막 · 잘 되는지 테스트

1. 배포된 사이트에서 **무료 상담 문의** 폼을 실제로 한 번 제출
2. 확인 2곳:
   - **메일**: kkjjkim@gmail.com 로 `[새 상담 신청] 이름 (연락처)` 메일이 오는지
     (안 보이면 **스팸함**도 확인)
   - **DB**: Supabase → Table Editor → `leads` 에 방금 신청이 한 줄 생겼는지
3. 둘 다 확인되면 완료! 🎉

---

## 자주 묻는 것

- **메일이 안 와요** → ① 스팸함 확인 ② `RESEND_API_KEY`/`LEAD_NOTIFY_TO` 오타 확인
  ③ Resend 가입 메일과 받는 메일이 같은지 확인 (무료 플랜 제약)
- **저장이 안 돼요** → Supabase 키 오타, 또는 `schema.sql`을 안 돌렸을 가능성.
  Table Editor에 `leads` 테이블이 보이는지부터 확인.
- **메일만/저장만 쓰고 싶어요** → 해당 키만 넣으면 됩니다. 한쪽 키가 없으면 그 기능만 조용히 꺼져요.
- **신청이 많아지면?** → 지금은 Supabase 대시보드에서 직접 봅니다. 나중에 관리자 목록 화면이
  필요하면 만들어 드릴 수 있어요.
